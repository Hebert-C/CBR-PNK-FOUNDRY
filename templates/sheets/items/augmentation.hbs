<form autocomplete="off" class="augCard {{#if system.isGLICHED}}GLICHED{{/if}}">
    <div class="header">
        <h1><input type="text" name="name" value="{{item.name}}"/></h1>
        <input name="system.isGLICHED" type="checkbox" id="{{item.id}}_G" {{#if system.isGLICHED }}checked{{/if}}/>
        <label class="item-glitch" for="{{item.id}}_G">{{localize 'RUNNER.GLICHED'}}</label>
    </div>
    <div class="desc">
        {{editor enrichedDesc target="system.desc" button=true editable=editable}}
    </div>
</form>